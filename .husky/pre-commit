#!/bin/sh
cd "$(dirname "$0")/.." || exit 1

# Type-check the project once using full tsconfig
npm run check-types || exit 1

# Run lint-staged for targeted lint fixes
npx --no lint-staged --concurrent false
